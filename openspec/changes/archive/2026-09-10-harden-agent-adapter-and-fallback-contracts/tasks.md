## 1. Testing

- [x] 1.1 Add a failing fallback test for retryable provider text on stdout.
- [x] 1.2 Add a failing generic-adapter test proving configured arguments occur once.
- [x] 1.3 Add contract tests for fresh model commands and non-retryable exits.

## 2. Adapter and fallback implementation

- [x] 2.1 Define the canonical command-construction contract for complete and
  adapter-specific command arguments.
- [x] 2.2 Classify retryable failures from bounded combined output.
- [x] 2.3 Preserve structured attempt evidence and fresh model/session behavior.

## 3. Documentation and verification

- [x] 3.1 Document fallback classification and generic adapter argument rules.
- [x] 3.2 Run focused tests, full tests, lint, compile checks, and strict OpenSpec validation.
