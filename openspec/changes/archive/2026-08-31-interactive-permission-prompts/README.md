# interactive-permission-prompts

When an agent run is blocked on a permission request or unresolved choice and a
terminal is available, prompt the user for the decision and feed it back so the
loop can continue, instead of always stopping and waiting for a manual resume.
